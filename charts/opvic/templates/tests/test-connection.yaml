apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opvic.fullname" . }}-test-connection"
  labels:
    {{- include "opvic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opvic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
